_schema-version: "3.3"
ID: s4-pp-mcp-app
version: 1.0.0
description: MCP server for SAP S/4HANA Business Partner with Principal Propagation

modules:

  # -------------------------
  # App Router (PUBLIC ENTRY)
  # -------------------------
  - name: s4-pp-mcp-approuter
    type: approuter.nodejs
    path: approuter
    parameters:
      memory: 256M
      disk-quota: 256M
      routes:
        - route: s4-pp-mcp-${space}.${default-domain}
    requires:
      - name: s4-pp-mcp-xsuaa
      - name: s4-pp-mcp-server-api
    properties:
      destinations: >
        [
          {
            "name": "s4-pp-mcp-backend",
            "url": "https://s4-pp-mcp-server-dev.cfapps.ap10.hana.ondemand.com",
            "forwardAuthToken": true
          }
        ]

  # -------------------------
  # MCP Server (BUSINESS LOGIC)
  # -------------------------
  - name: s4-pp-mcp-server
    type: nodejs
    path: .
    parameters:
      memory: 512M
      disk-quota: 512M
      routes:
        - route: s4-pp-mcp-server-${space}.${default-domain}
    requires:
      - name: s4-pp-mcp-xsuaa
      - name: s4-pp-mcp-destination
      - name: s4-pp-mcp-connectivity
    properties:
      APP_URL: https://s4-pp-mcp-${space}.${default-domain}
    provides:
      - name: s4-pp-mcp-server-api
        properties:
          url: https://${default-host}

resources:

  # -------------------------
  # XSUAA (AUTH)
  # -------------------------
  - name: s4-pp-mcp-xsuaa
    type: org.cloudfoundry.managed-service
    parameters:
      service: xsuaa
      service-plan: application
      path: ./xs-security.json

  # -------------------------
  # DESTINATION (PRINCIPAL PROPAGATION)
  # -------------------------
  - name: s4-pp-mcp-destination
    type: org.cloudfoundry.managed-service
    parameters:
      service: destination
      service-plan: lite

  # -------------------------
  # CONNECTIVITY (CLOUD CONNECTOR)
  # -------------------------
  - name: s4-pp-mcp-connectivity
    type: org.cloudfoundry.managed-service
    parameters:
      service: connectivity
      service-plan: lite

build-parameters:
  before-all:
    - builder: custom
      commands:
        - npm ci
        - npm run build